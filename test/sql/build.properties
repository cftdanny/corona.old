# HSQLDB JDBC Driver, eg. hsqldb-x.x.x.jar
driver = hsqldb-2.2.2.jar

# the generated database name and version
database = testdb
version = 1.0.M1

# the SQL script to create HSQLDB database
script = testdb.sql
