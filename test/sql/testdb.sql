SET SCHEMA PUBLIC;

CREATE TABLE PUBLIC.TORDMST(
	ORDRID BIGINT GENERATED BY DEFAULT AS IDENTITY, 
	ORDRNO CHAR(10), 
	ORDQTY INT,
	ORDDAT DATE WITH TIME ZONE,
	PRIMARY KEY(ORDRID)
);

SHUTDOWN;